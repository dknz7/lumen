import Section from "./Section";
export default function AccountsServers() {
  return <Section title="Accounts & Servers" description="Plex account and server overrides.">(Task 11 populates)</Section>;
}
